[sqlfluff]
dialect = postgres
max_line_length = 80
indent_unit = tab # Or space
tab_space_size = 4

[sqlfluff:rules]
# Enable or disable specific rules
# For example, to enable rule L003 (indentation) and disable L004 (trailing commas)
enable_rules = L003
disable_rules = L004


